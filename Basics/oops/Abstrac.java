/* 
 
 Abstract method -> provides only defination not implementation

 Abstract class -> if class contains one or more abstract method it should declare as abstract class
 -> this classes have not objects because in this only the declare method so obj not able to call that one
 -> cannot create abstract constructors
 -> cannot create abstract static methods -> static not support overloadings

 -> any child class extend abstract class it overriding all abstracts in order to provide implementation


 Abstract class can contains
 -> normal constructors, methods, static methods


 */